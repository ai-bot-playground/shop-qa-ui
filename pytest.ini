[pytest]
# `pythonpath = .` — testy importują `from src import agent`, a katalog shop-qa-ui
# nie jest instalowanym pakietem. Bez tego działa tylko `python -m pytest`
# (bo -m dokłada cwd do sys.path), a samo `pytest` wywala ModuleNotFoundError.
pythonpath = .
testpaths = tests
