apiVersion: v1
kind: Service
metadata:
  name: shop-qa-ui
  namespace: shop-qa-ui
  labels:
    app.kubernetes.io/name: shop-qa-ui
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: shop-qa-ui
  ports:
    - name: http
      port: 80
      targetPort: http
